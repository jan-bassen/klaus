/**
 * Text-to-speech output via the `tts` model tier.
 * Returns a Buffer containing the audio clip to be sent as a WhatsApp voice message.
 */
export async function textToSpeech(_text: string): Promise<Buffer> {
  throw new Error('TODO: not implemented');
}
